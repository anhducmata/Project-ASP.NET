@model IEnumerable<ProjectMVC.Models.Suplier>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SuplierName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SuplierAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SuplierDescription)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SuplierName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SuplierAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SuplierDescription)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.SuplierId }) |
                @Html.ActionLink("Details", "Details", new { id=item.SuplierId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.SuplierId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
